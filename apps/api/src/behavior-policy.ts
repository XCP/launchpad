/** XCP-69 assets are divisible. A sub-token remainder, or at most one percent
 * of what an address acquired, is dust rather than a meaningful open bag. */
export const EXIT_DUST_RAW = 100_000_000;
export const EXIT_DUST_DIVISOR = 100;

